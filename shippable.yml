language: java

build_image: java/openjdk-8-jdk

before_install:
   - echo "Before Install"

install:
   - echo "Install Step"
   - apt-get install maven


after_install:
   - echo "After Install"

before_script:
   - echo "Before Script"

# script: Not Required, Use default Java build

after_success:
   - echo "SUCCESS"
   - mvn package

after_failure:
   - echo "FAILURE"

after_script:
   - echo "After Script"

notifications:
      email:
          recipients:
          - techyugadi@gmail.com
